# Working Agreements

- 默认使用中文交流。
- 不要用关键词匹配、硬编码规则或前端启发式去限制 LLM 对用户需求的理解。
- Prompt 只描述可用工具、输入上下文、输出协议和安全边界；不要写自然语言需求到具体动作的规则表。
- 让 LLM 自行决定意图、目标范围、组件选择器、颜色语义和补丁参数；前端只负责校验结构化输出并执行。
- 不要用模块实现的代码污染全局模块代码。
